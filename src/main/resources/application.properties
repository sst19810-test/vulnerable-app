# VULNERABILITY: Debug mode enabled in production
debug=true
logging.level.root=DEBUG

# VULNERABILITY: Exposing sensitive endpoints
management.endpoints.web.exposure.include=*

# VULNERABILITY: CORS enabled for all origins
cors.allowed.origins=*

# Server configuration
server.port=1201
spring.application.name=vulnerable-app

# VULNERABILITY: Sensitive information in properties
app.admin.username=admin
app.admin.password=admin123
app.api.key=sk-1234567890abcdef
app.secret.token=mySecretToken123

# VULNERABILITY: SSL disabled
server.ssl.enabled=false
